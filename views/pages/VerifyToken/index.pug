extends ../../layout.pug
block initial_content
  include ../../components/mixins/formInput.pug
  include ../../components/mixins/formButton.pug
  include ../../components/mixins/anchorButton.pug

  div(class="flex justify-center items-center h-screen flex-col")
    div(class="relative flex justify-center rounded-md shadow-md bg-white w-80 h-60 p-6 py-5")
      if status
        div(class="absolute flex justify-center items-center w-full")
          h1(class="text-lg font-medium text-red-500")= message
      form(class="flex flex-col justify-center items-center h-full w-full gap-3 " action="/verifyToken" method="POST")
        +formInput("Clave de 6 dígitos", "token", "sanitizeTicket", "6", "6", "text")
        div(class="mt-5 w-full")
          +formButton("Verificar")
